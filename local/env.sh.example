# Copia a env.sh y ejecuta: source local/env.sh
# Así la caché de Hugging Face queda bajo local/.cache/

export HF_HOME="$(cd "$(dirname "$0")" && pwd)/.cache/huggingface"
export TRANSFORMERS_CACHE="$HF_HOME"
export SENTENCE_TRANSFORMERS_HOME="$HF_HOME"
