# Entorno Python en local/ — núcleo (compatible con Python 3.10–3.14)
# Instalar: pip install -r requirements.txt   (desde local/)  o  pip install -r local/requirements.txt

numpy>=1.24.0
scikit-learn>=1.3.0
joblib>=1.3.0
tqdm>=4.66.0

# Embeddings con sentence-transformers: ver requirements-embeddings.txt
# (requiere PyTorch; en Python 3.14 aún no hay wheels estables de torch)
