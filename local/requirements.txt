# Woz (local/) — scripts usan solo biblioteca estándar de Python 3.10+.
# No hay dependencias pip obligatorias para clasificación vía Ollama.
#
# Opcional (venv para aislamiento):
#   python3 -m venv local/venv && source local/venv/bin/activate
